// Canonical application-shell public boundary.
//
// The widget implementation remains in `main.dart` during staged extraction,
// but consumers can import this stable path immediately.
export '../main.dart' show KlipioApp;
